<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BarcodeScanner" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimaryDark">@color/black</item>
    </style>

    <style name="Theme.BarcodeScanner" parent="Base.Theme.BarcodeScanner" />
</resources>